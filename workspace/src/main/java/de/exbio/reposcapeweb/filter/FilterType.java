package de.exbio.reposcapeweb.filter;

public enum FilterType {
    DOMAIN_ID, DISPLAY_NAME,ICD10, SYNONYM, IUPAC, SYMBOLS, ORIGIN,
    DESCRIPTION, INDICATION, COMMENTS,
    TYPE, CATEGORY, GROUP,
}
